const axios = require("axios");
const moment = require("moment-timezone");
const {
    bold,
    quote
} = require("@mengkodingan/ckptw");

module.exports = {
    axios,
    moment,
    bold,
    quote
};